# EDA Precision Lab — Week 06 · Day 1

Real Relationships vs. Real Comparisons, and Verified Findings.

## Dataset
`students.csv` — 600 rows × 6 columns, generated by `generate_data.py`
(verbatim assignment spec, seed=21). SHA-256 fingerprint recorded in the notebook.

## Deliverables
- `week6_day1_precision_lab.ipynb` — executed notebook, kata steps 1–8
- Genuine relationship chart + correlation finding (study hours)
- Null relationship chart written up as a real finding (sleep hours)
- Categorical comparison chart with shuffle-test justification
- Line-chart trap built deliberately + false impression named
- Layout collision built, confirmed in saved PNG, then fixed
- Deliberate color/order choices on the comparison chart
- Self-audit table: claimed numbers vs freshly recomputed numbers

## How to run
```bash
python -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/bin/python generate_data.py
.venv/bin/jupyter nbconvert --to notebook --execute --inplace week6_day1_precision_lab.ipynb
```
